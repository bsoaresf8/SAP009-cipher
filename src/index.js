import cipher from './cipher.js';

document.getElementById('id botao').addEventListener(click, function cifrar)

function cifrar(){
document.getelementbyid('id input').value
  let msg

console.log(cipher);
